import { BrDatePipePipe } from './br-date-pipe.pipe';

describe('BrDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new BrDatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
